<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComposeStudyApplication"
        tools:targetApi="31">
        <activity
            android:name=".TextfiledActivity"
            android:exported="false"
            android:label="@string/title_activity_textfiled"
            android:theme="@style/Theme.ComposeStudyApplication" />
        <activity
            android:name=".SliderActivity"
            android:exported="false"
            android:label="@string/title_activity_slider"
            android:theme="@style/Theme.ComposeStudyApplication" />
        <activity
            android:name=".ButtonActivity"
            android:exported="true"
            android:label="@string/title_activity_button"
            android:theme="@style/Theme.ComposeStudyApplication" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DialogActivity"
            android:exported="false"
            android:label="@string/title_activity_dialog"
            android:theme="@style/Theme.ComposeStudyApplication" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ComposeStudyApplication">

        </activity>
    </application>

</manifest>